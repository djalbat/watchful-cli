"use strict";

const DEFAULT_WAIT = 0,
      DEFAULT_QUIETLY = false,
      DEFAULT_METRICS = false,
      DEFAULT_PROCESSES = 0;

module.exports = {
  DEFAULT_WAIT,
  DEFAULT_QUIETLY,
  DEFAULT_METRICS,
  DEFAULT_PROCESSES
};
